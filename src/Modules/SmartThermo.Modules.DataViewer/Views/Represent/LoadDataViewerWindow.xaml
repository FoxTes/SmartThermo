<UserControl
    x:Class="SmartThermo.Modules.DataViewer.Views.Represent.LoadDataViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enum="clr-namespace:SmartThermo.Core.Enums;assembly=SmartThermo.Core"
    xmlns:local="clr-namespace:SmartThermo.Modules.DataViewer.Views.Charts"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    ui:ThemeManager.RequestedTheme="Dark"
    Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="Chart0">
            <Grid Margin="0,8,8,8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 11"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[0]}" />
                        <lvc:LineSeries
                            Title="Д 12"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[1]}" />
                        <lvc:LineSeries
                            Title="Д 13"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[2]}" />
                        <lvc:LineSeries
                            Title="Д 14"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[3]}" />
                        <lvc:LineSeries
                            Title="Д 15"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[4]}" />
                        <lvc:LineSeries
                            Title="Д 16"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[5]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[0], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[0], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[0], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[0], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[0].HysteresisThreshold1}" Value="{Binding LimitRelayItems[0].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[0].HysteresisThreshold2}" Value="{Binding LimitRelayItems[0].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>1</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>1</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data0">
            <Grid Margin="0,8,8,8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д11" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[0].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[0].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[0].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[0].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д12" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[1].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[1].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[1].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[1].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д13" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[2].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[2].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[2].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[2].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д14" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[3].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[3].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[3].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[3].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д15" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[4].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[4].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[4].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[4].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д16" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[5].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[5].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[5].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[5].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>1</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="Chart1">
            <Grid Margin="8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 21"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[6]}" />
                        <lvc:LineSeries
                            Title="Д 22"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[7]}" />
                        <lvc:LineSeries
                            Title="Д 23"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[8]}" />
                        <lvc:LineSeries
                            Title="Д 24"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[9]}" />
                        <lvc:LineSeries
                            Title="Д 25"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[10]}" />
                        <lvc:LineSeries
                            Title="Д 26"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[11]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[1], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[1], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[1], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[1], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[1].HysteresisThreshold1}" Value="{Binding LimitRelayItems[1].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[1].HysteresisThreshold2}" Value="{Binding LimitRelayItems[1].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>2</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>2</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data1">
            <Grid Margin="8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д21" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[6].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[6].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[6].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[6].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д22" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[7].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[7].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[7].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[7].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д23" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[8].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[8].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[8].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[8].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д24" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[9].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[9].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[9].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[9].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д25" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[10].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[10].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[10].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[10].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д26" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[11].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[11].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[11].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[11].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>2</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="Chart2">
            <Grid Margin="8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 31"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[12]}" />
                        <lvc:LineSeries
                            Title="Д 32"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[13]}" />
                        <lvc:LineSeries
                            Title="Д 33"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[14]}" />
                        <lvc:LineSeries
                            Title="Д 34"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[15]}" />
                        <lvc:LineSeries
                            Title="Д 35"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[16]}" />
                        <lvc:LineSeries
                            Title="Д 36"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[17]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[2], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[2], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[2], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[2], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[2].HysteresisThreshold1}" Value="{Binding LimitRelayItems[2].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[2].HysteresisThreshold2}" Value="{Binding LimitRelayItems[2].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>3</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>3</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data2">
            <Grid Margin="8" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д31" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[12].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[12].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[12].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[12].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д32" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[13].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[13].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[13].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[13].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д33" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[14].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[14].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[14].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[14].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д34" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[15].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[15].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[15].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[15].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д35" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[16].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[16].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[16].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[16].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д36" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[17].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[17].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[17].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[17].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>3</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="Chart3">
            <Grid Margin="0,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 41"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[18]}" />
                        <lvc:LineSeries
                            Title="Д 42"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[19]}" />
                        <lvc:LineSeries
                            Title="Д 43"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[20]}" />
                        <lvc:LineSeries
                            Title="Д 44"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[21]}" />
                        <lvc:LineSeries
                            Title="Д 45"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[22]}" />
                        <lvc:LineSeries
                            Title="Д 46"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[23]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[3], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[3], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[3], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[3], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[3].HysteresisThreshold1}" Value="{Binding LimitRelayItems[3].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[3].HysteresisThreshold2}" Value="{Binding LimitRelayItems[3].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>4</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>4</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data3">
            <Grid Margin="0,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д41" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[18].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[18].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[18].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[18].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д42" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[19].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[19].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[19].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[19].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д43" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[20].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[20].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[20].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[20].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д44" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[21].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[21].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[21].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[21].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д45" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[22].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[22].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[22].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[22].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д46" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[23].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[23].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[23].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[23].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>4</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="Chart4">
            <Grid Margin="8,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 51"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[24]}" />
                        <lvc:LineSeries
                            Title="Д 52"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[25]}" />
                        <lvc:LineSeries
                            Title="Д 53"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[26]}" />
                        <lvc:LineSeries
                            Title="Д 54"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[27]}" />
                        <lvc:LineSeries
                            Title="Д 55"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[28]}" />
                        <lvc:LineSeries
                            Title="Д 56"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[29]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[4], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[4], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[4], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[4], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[4].HysteresisThreshold1}" Value="{Binding LimitRelayItems[4].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[4].HysteresisThreshold2}" Value="{Binding LimitRelayItems[4].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>5</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>5</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data4">
            <Grid Margin="8,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д51" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[24].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[24].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[24].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[24].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д52" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[25].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[25].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[25].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[25].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д53" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[26].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[26].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[26].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[26].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д54" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[27].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[27].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[27].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[27].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д55" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[28].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[28].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[28].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[28].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д56" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[29].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[29].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[29].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[29].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>5</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="Chart5">
            <Grid Margin="8,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                <lvc:CartesianChart
                    Margin="8"
                    DisableAnimations="True"
                    FontSize="12"
                    Hoverable="False"
                    LegendLocation="Top"
                    Zoom="Y">
                    <lvc:CartesianChart.Series>
                        <lvc:LineSeries
                            Title="Д 61"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#003f5c"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[30]}" />
                        <lvc:LineSeries
                            Title="Д 62"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#444e86"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[31]}" />
                        <lvc:LineSeries
                            Title="Д 63"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#955196"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[32]}" />
                        <lvc:LineSeries
                            Title="Д 64"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#dd5182"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[33]}" />
                        <lvc:LineSeries
                            Title="Д 65"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ff6e54"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[34]}" />
                        <lvc:LineSeries
                            Title="Д 66"
                            Fill="Transparent"
                            LineSmoothness="1"
                            PointGeometry="{x:Null}"
                            Stroke="#ffa600"
                            StrokeThickness="2"
                            Values="{Binding ChartValues[35]}" />
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding YFormatter}"
                            MaxRange="{Binding AxisYMax[5], Mode=TwoWay}"
                            MaxValue="{Binding AxisYMax[5], Mode=TwoWay}"
                            MinRange="{Binding AxisYMin[5], Mode=TwoWay}"
                            MinValue="{Binding AxisYMin[5], Mode=TwoWay}"
                            Unit="1">
                            <lvc:Axis.Sections>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[5].HysteresisThreshold1}" Value="{Binding LimitRelayItems[5].TemperatureThreshold1}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="red" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                                <lvc:AxisSection SectionWidth="{Binding LimitRelayItems[5].HysteresisThreshold2}" Value="{Binding LimitRelayItems[5].TemperatureThreshold2}">
                                    <lvc:AxisSection.Fill>
                                        <SolidColorBrush Opacity=".25" Color="Green" />
                                    </lvc:AxisSection.Fill>
                                </lvc:AxisSection>
                            </lvc:Axis.Sections>
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis
                            DisableAnimations="True"
                            FontSize="12"
                            LabelFormatter="{Binding XFormatter}"
                            MaxValue="{Binding AxisMax}"
                            MinValue="{Binding AxisMin}"
                            Unit="{Binding AxisUnit}">
                            <lvc:Axis.Separator>
                                <lvc:Separator
                                    Step="{Binding AxisStep}"
                                    Stroke="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                                    StrokeThickness="1" />
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.Style>
                        <Style TargetType="lvc:CartesianChart">
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>6</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding ResetScalingChartCommand}" Header="Сбросить масштаб">
                                            <MenuItem.CommandParameter>
                                                <sys:Int32>6</sys:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </lvc:CartesianChart.Style>
                    <lvc:CartesianChart.DataTooltip>
                        <local:CustomersTooltip />
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="Data5">
            <Grid Margin="8,8,8,0" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="8" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д61" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[30].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[30].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[30].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[30].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д62" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[31].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[31].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[31].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[31].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д63" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[32].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[32].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[32].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="2"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[32].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д64" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[33].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[33].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[33].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="0"
                    Margin="8,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[33].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д65" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[34].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[34].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[34].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="4,8,4,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[34].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <ui:SimpleStackPanel
                    Grid.Row="3"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="4">
                    <TextBlock
                        Foreground="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"
                        Style="{DynamicResource SubtitleTextBlockStyle}"
                        Text="Д66" />
                    <Ellipse
                        Width="16"
                        Height="16"
                        Margin="0,2,0,0">
                        <Ellipse.Style>
                            <Style TargetType="{x:Type Ellipse}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SensorsData[35].StatusSensor}" Value="{x:Static enum:StatusSensor.Online}">
                                        <Setter Property="Fill" Value="#2dbe60" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[35].StatusSensor}" Value="{x:Static enum:StatusSensor.Wait}">
                                        <Setter Property="Fill" Value="#fdd340" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SensorsData[35].StatusSensor}" Value="{x:Static enum:StatusSensor.Offline}">
                                        <Setter Property="Fill" Value="#eb4b4e" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </ui:SimpleStackPanel>
                <Border
                    Grid.Row="4"
                    Grid.Column="2"
                    Margin="4,8,8,8"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                    CornerRadius="2">
                    <Viewbox MaxHeight="128">
                        <TextBlock
                            Margin="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding SensorsData[35].Temperature, StringFormat={}{0}°C, TargetNullValue='-/-'}"
                            TextWrapping="Wrap" />
                    </Viewbox>
                </Border>

                <Grid.Style>
                    <Style TargetType="{x:Type Grid}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Command="{Binding ChangeSelectModeCommand}" Header="Изменить режим отображения">
                                        <MenuItem.CommandParameter>
                                            <sys:Int32>6</sys:Int32>
                                        </MenuItem.CommandParameter>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Style>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Margin="0,8,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ContentControl Grid.Column="0" Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[0]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart0}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[0]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data0}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
            <ContentControl
                Grid.Row="0"
                Grid.Column="1"
                Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[1]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart1}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[1]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data1}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
            <ContentControl
                Grid.Row="0"
                Grid.Column="2"
                Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[2]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart2}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[2]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data2}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>

            <ContentControl
                Grid.Row="1"
                Grid.Column="0"
                Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[3]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart3}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[3]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data3}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
            <ContentControl
                Grid.Row="1"
                Grid.Column="1"
                Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[4]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart4}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[4]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data4}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
            <ContentControl
                Grid.Row="1"
                Grid.Column="2"
                Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectMode[5]}" Value="False">
                                <Setter Property="ContentTemplate" Value="{StaticResource Chart5}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectMode[5]}" Value="True">
                                <Setter Property="ContentTemplate" Value="{StaticResource Data5}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Grid>

        <Border
            Grid.Column="1"
            Margin="0,16,0,0"
            Background="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}" />
        <DataGrid
            Grid.Column="2"
            Padding="16,16,0,0"
            AutoGenerateColumns="False"
            Background="Transparent"
            CanUserResizeColumns="False"
            CanUserResizeRows="False"
            GridLinesVisibility="All"
            HorizontalScrollBarVisibility="Disabled"
            IsReadOnly="True"
            ItemsSource="{Binding SensorsEtherItems}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="auto"
                    Binding="{Binding Path=Id}"
                    Header="Номер" />
                <DataGridTextColumn
                    Width="auto"
                    Binding="{Binding Path=Time}"
                    Header="Выход в эфир (сек)" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
